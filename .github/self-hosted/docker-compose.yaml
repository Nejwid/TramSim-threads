version: '3.8'

services:
  runner:
    image: runner:latest # You can use this image or build your own
    restart: always
    environment:
      - REPO_URL=https://github.com/Nejwid/TramSim-threads
      - RUNNER_TOKEN=A6LUGTI3UP7BIBVWSAL5BPDJRT3DU
      - NAME=self-hosted-Latitude7410